import React, { Fragment } from 'react';
import Header from '../Header';
import About from '../About';

const Home = () => (
  <Fragment>
    <Header />
    <About />
  </Fragment>
);

export default Home;
